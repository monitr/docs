import { PrimaryButton } from "@fluentui/react";

1. Select your budget version
1. Set a period range
1. All budget accounts are automatically filled in with 0 values per month. Adjust the numbers to your preference.
1. Click <PrimaryButton> Save </PrimaryButton>